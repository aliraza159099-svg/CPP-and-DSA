// main.cpp
#include "student.h" // main only needs to see the blueprint!

int main() {
    // Creating an object of the Student class
    Student s1("Ali", 101);

    s1.displayDetails(); // Calling the method
    return 0;
}
